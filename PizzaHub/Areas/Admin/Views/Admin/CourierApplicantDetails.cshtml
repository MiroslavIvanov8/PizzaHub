@model PizzaHub.Core.ViewModels.Courier.CourierApplicantModel

@{
    ViewData["Title"] = "Courier Applicant Details";
}

<h1>Courier Applicant Details</h1>

<div>
    <dl class="row">
        <dt class="col-sm-2">Full Name:</dt>
        <dd class="col-sm-10">@Model.FullName</dd>

        <dt class="col-sm-2">Age:</dt>
        <dd class="col-sm-10">@Model.Age</dd>

        <dt class="col-sm-2">Email:</dt>
        <dd class="col-sm-10">@Model.Email</dd>

        <dt class="col-sm-2">Phone Number:</dt>
        <dd class="col-sm-10">@Model.PhoneNumber</dd>

        <dt class="col-sm-2">Description:</dt>
        <dd class="col-sm-10">@Model.Description</dd>
    </dl>
</div>

<div>
    <a asp-action="Approve" asp-route-id="@Model.Id" class="btn btn-success">Approve</a>
    <a asp-action="Decline" asp-route-id="@Model.Id" class="btn btn-danger">Decline</a>
</div>