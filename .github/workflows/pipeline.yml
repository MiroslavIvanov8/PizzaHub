name: Build & Test

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs: 
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4  

    - name: Setup .NET
      uses: actions/setup-dotnet@v4  
      with:
        dotnet-version: 8.0.x

    # Restore dependencies
    - name: Restore dependencies
      run: dotnet restore PizzaHub/PizzaHub.csproj

    # Build the solution
    - name: Build
      run: dotnet build PizzaHub/PizzaHub.csproj --no-restore  
    # Run tests
    - name: Test
      run: dotnet test PizzaHub.UnitTests/PizzaHub.UnitTests.csproj --no-build --verbosity normal  
